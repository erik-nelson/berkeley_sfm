language: cpp

compiler:
  - gcc
  - clang

before_install:
  - sudo apt-get update -qq

install:
  - add-apt-repository -y ppa:tuleu/precise-backports
  - apt-get -y update
  - apt-get install wget git curl python-dev python-numpy python3-dev libleveldb-dev libsnappy-dev libopencv-dev libgflags-dev libgoogle-glog-dev

before_script:
  - mkdir build
  - cd build
  - cmake -DCMAKE_BUILD_TYPE=Release ..

script: make
